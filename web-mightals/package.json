{
  "name": "web-mightals",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch",
    "test": "vitest",
    "test:integration": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
